# Runtime: no third-party dependencies (Python 3.11+ stdlib only).
# Installs the library and CLI in editable mode:
#   pip install -r requirements.txt
#
# For tests and coverage, use requirements-dev.txt instead:
#   pip install -r requirements-dev.txt
-e .
