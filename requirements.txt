# Runtime: zero third-party dependencies (Python 3.11+ stdlib only).
#
# Editable install of the library and `simplequeue` CLI:
#   pip install -r requirements.txt
#
# For tests, coverage, mypy, and ruff, use requirements-dev.txt:
#   pip install -r requirements-dev.txt

-e .
